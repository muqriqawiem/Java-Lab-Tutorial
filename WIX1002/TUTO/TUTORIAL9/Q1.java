package WIX1002.TUTO.TUTORIAL9;

public class Q1 {

    public static void main(String[] args) {
        /*
        1a. Write a static method compare that return true if two objects parameter (Student s, Teacher
t) are belongs to the same class. Otherwise return false.
        
public static boolean Compare(Student s, Teacher t){
if(s.getClass() == t.getClass()){
return true;
} else{
return false;
}
}
        
        
1b. Write a static method isClass that return true if the object parameter (Student s) is belong to
any descendent class of Person. Otherwise return false.
        
public static boolean isClass(Student s){
if(s instanceof Person){
return true;
} else {
return false;
}
}

        */
    }
    
}
